# base
numpy==1.19.5
Pillow==8.4.0
opencv-python==4.5.5.64
pyyaml
flask
tqdm
scikit-learn==0.24.2
matplotlib==3.3.4
protobuf==3.19.6

# Jetson/L4T note:
# - torch / torchvision 은 l4t-ml:r32.7.1-py3 컨테이너에 이미 포함된 버전을 사용한다.
# - TensorRT / pycuda 도 컨테이너 기본 제공을 사용한다.
# - torch2trt 는 이 리포지토리에 포함되어 있으므로 별도 설치 없이
#   컨테이너 안에서:  `cd /workspace/torch2trt && python3 setup.py install`
#   을 한 번 실행해 준다.
